services:
  - type: web
    name: predictor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_pwa:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0